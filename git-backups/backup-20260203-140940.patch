diff --git a/Modules/Admin/Livewire/Chat/ChatManager.php b/Modules/Admin/Livewire/Chat/ChatManager.php
index 63dac11..b249a5e 100644
--- a/Modules/Admin/Livewire/Chat/ChatManager.php
+++ b/Modules/Admin/Livewire/Chat/ChatManager.php
@@ -21,6 +21,7 @@ public function getListeners()
             // Lắng nghe từ Channel 'chat' (phát ra từ NodeJS)
             "echo:chat,MessageSent" => 'handleIncomingMessage',
             "refresh-chat" => '$refresh',
+            'refresh-widget' => '$refresh',
         ];
     }
 
@@ -80,4 +81,10 @@ public function render()
                 : null,
         ]);
     }
+
+    public function delete($id, ChatService $service)
+    {
+        $service->deleteMessage($id);
+        $this->dispatch('refresh-chat');
+    }
 }
diff --git a/Modules/Admin/Services/ChatService.php b/Modules/Admin/Services/ChatService.php
index 29612ff..5d6c2d8 100644
--- a/Modules/Admin/Services/ChatService.php
+++ b/Modules/Admin/Services/ChatService.php
@@ -78,4 +78,26 @@ protected function broadcastToNodeJS(array $payload): void
             Log::error("Realtime Bridge Failure: " . $e->getMessage());
         }
     }
+    public function deleteMessage($messageId): bool
+    {
+        return DB::transaction(function () use ($messageId) {
+            $message = ChatMessage::find($messageId);
+            if (!$message) return false;
+
+            $sessionId = $message->chat_session_id;
+            $message->delete(); // Xóa hẳn khỏi DB theo yêu cầu
+
+            // Thông báo cho các bên để xóa tin nhắn trên UI
+            $this->broadcastToNodeJS([
+                'channel' => 'chat',
+                'event'   => 'MessageDeleted',
+                'data'    => [
+                    'message_id' => $messageId,
+                    'session_id' => $sessionId
+                ]
+            ]);
+
+            return true;
+        });
+    }
 }
diff --git a/Modules/Admin/resources/views/livewire/chat/chat-manager.blade.php b/Modules/Admin/resources/views/livewire/chat/chat-manager.blade.php
index e3bf8db..0d12a3a 100644
--- a/Modules/Admin/resources/views/livewire/chat/chat-manager.blade.php
+++ b/Modules/Admin/resources/views/livewire/chat/chat-manager.blade.php
@@ -6,9 +6,8 @@
 
         <div class="overflow-y-auto flex-1 custom-scrollbar">
             @forelse($sessions as $session)
-                <div wire:click="selectSession({{ $session->id }})"
-                     wire:key="session-{{ $session->id }}"
-                     class="p-4 cursor-pointer transition-all border-b border-gray-50 hover:bg-white {{ $activeSessionId == $session->id ? 'bg-white shadow-md !border-l-4 !border-l-blue-600' : '' }}">
+                <div wire:click="selectSession({{ $session->id }})" wire:key="session-{{ $session->id }}"
+                    class="p-4 cursor-pointer transition-all border-b border-gray-50 hover:bg-white {{ $activeSessionId == $session->id ? 'bg-white shadow-md !border-l-4 !border-l-blue-600' : '' }}">
                     <div class="flex justify-between items-start">
                         <span class="font-semibold text-gray-700">{{ $session->display_name }}</span>
                         <span class="text-[10px] text-gray-400">{{ $session->last_message_at?->diffForHumans() }}</span>
@@ -24,10 +23,12 @@ class="p-4 cursor-pointer transition-all border-b border-gray-50 hover:bg-white
     </div>
 
     <div class="w-2/3 flex flex-col bg-white">
-        @if($activeSession)
-            <div class="p-4 border-b border-gray-100 flex items-center justify-between bg-white/80 backdrop-blur-md sticky top-0 z-10">
+        @if ($activeSession)
+            <div
+                class="p-4 border-b border-gray-100 flex items-center justify-between bg-white/80 backdrop-blur-md sticky top-0 z-10">
                 <div class="flex items-center gap-3">
-                    <div class="h-10 w-10 rounded-full bg-blue-100 flex items-center justify-center text-blue-600 font-bold">
+                    <div
+                        class="h-10 w-10 rounded-full bg-blue-100 flex items-center justify-center text-blue-600 font-bold">
                         {{ substr($activeSession->display_name, 0, 1) }}
                     </div>
                     <div>
@@ -39,37 +40,101 @@ class="p-4 cursor-pointer transition-all border-b border-gray-50 hover:bg-white
             </div>
 
             <div class="flex-1 p-6 overflow-y-auto space-y-4 bg-gray-50/30 custom-scrollbar" id="chat-window">
-                @foreach($activeSession->messages as $msg)
-                    <div wire:key="msg-{{ $msg->id }}" class="flex {{ $msg->sender_type == 'admin' ? 'justify-end' : 'justify-start' }}">
-                        <div class="max-w-[75%] p-3.5 rounded-2xl shadow-sm text-sm leading-relaxed
-                            {{ $msg->sender_type == 'admin'
-                                ? 'bg-blue-600 text-white rounded-tr-none'
-                                : 'bg-white text-gray-700 border border-gray-100 rounded-tl-none' }}">
+                @foreach ($activeSession->messages as $msg)
+                    <div wire:key="msg-{{ $msg->id }}" x-data="{ showDelete: false }" @mouseenter="showDelete = true"
+                        @mouseleave="showDelete = false"
+                        class="group flex items-end gap-2 mb-4 {{ $msg->sender_type == 'admin' ? 'flex-row-reverse' : 'flex-row' }}">
+
+                        <div
+                            class="relative max-w-[75%] p-3.5 rounded-2xl shadow-sm text-sm leading-relaxed transition-all
+                        {{ $msg->sender_type == 'admin'
+                            ? 'bg-blue-600 text-white rounded-tr-none'
+                            : 'bg-white text-gray-700 border border-gray-100 rounded-tl-none' }}">
+
                             {{ $msg->message }}
-                            <div class="text-[9px] mt-1 opacity-70 {{ $msg->sender_type == 'admin' ? 'text-right' : 'text-left' }}">
+
+                            <div
+                                class="text-[9px] mt-1 opacity-70 {{ $msg->sender_type == 'admin' ? 'text-right' : 'text-left' }}">
                                 {{ $msg->created_at->format('H:i') }}
                             </div>
                         </div>
+
+                        @if ($msg->sender_type == 'admin')
+                            <button x-show="showDelete" x-transition:enter="transition opacity-0 scale-90 duration-200"
+                                x-transition:enter-end="opacity-100 scale-100" wire:click="delete({{ $msg->id }})"
+                                wire:confirm="Bạn có chắc chắn muốn xóa vĩnh viễn tin nhắn này khỏi hệ thống?"
+                                class="p-2 text-gray-400 hover:text-red-500 transition-colors bg-gray-50 rounded-full shadow-sm"
+                                title="Xóa tin nhắn">
+                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
+                                        d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
+                                </svg>
+                            </button>
+                        @endif
                     </div>
                 @endforeach
             </div>
 
             <div class="p-4 border-t border-gray-100 bg-white">
-                <form wire:submit.prevent="send" class="flex gap-3 items-center">
-                    <input type="text" wire:model="message"
-                           class="flex-1 bg-gray-50 border-gray-200 rounded-xl py-3 px-4 text-sm focus:ring-2 focus:ring-blue-500 focus:bg-white transition-all outline-none"
-                           placeholder="Nhập nội dung phản hồi cho khách...">
-                    <button type="submit" wire:loading.attr="disabled"
-                            class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-blue-200 transition-all active:scale-95 disabled:opacity-50">
-                        <span wire:loading.remove>Gửi tin</span>
-                        <span wire:loading>...</span>
-                    </button>
-                </form>
+                <div x-data="{
+                    isTyping: false,
+                    timeout: null,
+                    sendTyping(status) {
+                        if (!window.socket) return;
+                        window.socket.emit('typing', {
+                            session_id: @js($activeSessionId),
+                            is_typing: status,
+                            sender_type: 'admin'
+                        });
+                    }
+                }">
+                    <div x-data="{ guestIsTyping: false }"
+                         @display-typing.window="if($event.detail.session_id == @js($activeSessionId) && $event.detail.sender_type != 'admin') guestIsTyping = $event.detail.is_typing"
+                         x-show="guestIsTyping"
+                         x-transition:enter="transition ease-out duration-200"
+                         class="px-4 py-2 text-xs text-gray-400 italic flex items-center gap-2">
+                        <span class="flex gap-1">
+                            <span class="animate-bounce">.</span>
+                            <span class="animate-bounce [animation-delay:0.2s]">.</span>
+                            <span class="animate-bounce [animation-delay:0.4s]">.</span>
+                        </span>
+                        Khách đang soạn tin...
+                    </div>
+
+                    <form wire:submit.prevent="send"
+                          @submit="sendTyping(false)"
+                          class="flex gap-3 items-center p-4 border-t border-gray-100 bg-white">
+
+                        <input type="text"
+                               wire:model="message"
+                               @keydown="
+                                    if(!isTyping) { isTyping = true; sendTyping(true); }
+                                    clearTimeout(timeout);
+                                    timeout = setTimeout(() => { isTyping = false; sendTyping(false); }, 3000);
+                               "
+                               class="flex-1 bg-gray-50 border-gray-200 rounded-xl py-3 px-4 text-sm focus:ring-2 focus:ring-blue-500 focus:bg-white transition-all outline-none"
+                               placeholder="Nhập nội dung phản hồi cho khách..."
+                               autocomplete="off">
+
+                        <button type="submit"
+                                wire:loading.attr="disabled"
+                                class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-xl font-bold text-sm shadow-lg shadow-blue-200 transition-all active:scale-95 disabled:opacity-50">
+                            <span wire:loading.remove>Gửi tin</span>
+                            <span wire:loading class="flex items-center gap-1">
+                                <svg class="animate-spin h-4 w-4 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg>
+                            </span>
+                        </button>
+                    </form>
+                </div>
             </div>
         @else
             <div class="flex-1 flex flex-col items-center justify-center text-gray-300">
                 <div class="w-24 h-24 bg-gray-50 rounded-full flex items-center justify-center mb-4">
-                    <svg class="w-12 h-12 opacity-20" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path></svg>
+                    <svg class="w-12 h-12 opacity-20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1"
+                            d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z">
+                        </path>
+                    </svg>
                 </div>
                 <p class="font-medium">Chọn một hội thoại để hỗ trợ khách hàng</p>
             </div>
@@ -78,39 +143,42 @@ class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-xl font-bold t
 </div>
 
 @push('scripts')
-<script>
-    document.addEventListener('livewire:initialized', () => {
-        const chatWindow = document.getElementById('chat-window');
-
-        const scrollToBottom = () => {
-            if (chatWindow) {
-                chatWindow.scrollTop = chatWindow.scrollHeight;
-            }
-        };
-
-        // Lắng nghe lệnh cuộn từ Livewire
-        window.addEventListener('scroll-chat-to-bottom', () => {
-            setTimeout(scrollToBottom, 50);
-        });
+    <script>
+        document.addEventListener('livewire:initialized', () => {
+            const chatWindow = document.getElementById('chat-window');
 
-        // Khởi tạo lần đầu
-        scrollToBottom();
-        // 1. Lắng nghe từ Echo (Cưỡng bức)
-        window.Echo.connector.socket.onAny((eventName, data) => {
-            if (eventName === 'MessageSent') {
-                console.log("📡 Admin nhận tín hiệu, đang làm mới UI...");
-
-                // Gọi trực tiếp vào component Livewire để refresh
-                // 'refresh-chat' là listener đã khai báo trong ChatManager.php
-                Livewire.dispatch('refresh-chat');
-
-                // Cuộn xuống sau khi UI đã render xong
-                setTimeout(() => {
-                    if(chatWindow) chatWindow.scrollTop = chatWindow.scrollHeight;
-                }, 300);
-            }
-        });
-    });
+            const scrollToBottom = () => {
+                if (chatWindow) {
+                    chatWindow.scrollTop = chatWindow.scrollHeight;
+                }
+            };
+
+            // Lắng nghe lệnh cuộn từ Livewire
+            window.addEventListener('scroll-chat-to-bottom', () => {
+                setTimeout(scrollToBottom, 50);
+            });
 
-</script>
+            // Khởi tạo lần đầu
+            scrollToBottom();
+            // 1. Lắng nghe từ Echo (Cưỡng bức)
+            window.Echo.connector.socket.onAny((eventName, data) => {
+                // Trường hợp 1: Có tin nhắn mới (Đã chạy)
+                if (eventName === 'MessageSent') {
+                    Livewire.dispatch('refresh-chat'); // hoặc 'refresh-widget'
+                }
+
+                // Trường hợp 2: Có tin nhắn bị xóa (Cần bổ sung)
+                if (eventName === 'MessageDeleted') {
+                    console.log("🗑️ Phát hiện tin nhắn bị xóa, đang cập nhật UI...");
+
+                    // Ép Livewire gọi lại hàm render() để lấy danh sách tin nhắn mới từ DB
+                    Livewire.dispatch('refresh-chat');
+
+                    // Hoặc nếu muốn mượt hơn, bạn có thể dùng JS xóa trực tiếp phần tử DOM
+                    // const el = document.querySelector(`[wire\\:key="msg-${data.message_id}"]`);
+                    // if(el) el.remove();
+                }
+            });
+        });
+    </script>
 @endpush
diff --git a/Modules/Website/Livewire/Chat/ChatWidget.php b/Modules/Website/Livewire/Chat/ChatWidget.php
index 44df5b1..09b7e8e 100644
--- a/Modules/Website/Livewire/Chat/ChatWidget.php
+++ b/Modules/Website/Livewire/Chat/ChatWidget.php
@@ -19,7 +19,9 @@ public function getListeners()
         return [
             // Lắng nghe tin nhắn mới từ Echo
             "echo:chat,MessageSent" => 'handleIncoming',
-            'refresh-widget' => '$refresh'
+            'refresh-widget' => '$refresh',
+            'refresh-chat' => '$refresh',
+
         ];
     }
 
@@ -55,7 +57,10 @@ public function startChat(ChatService $chatService)
         // Khởi tạo session thông qua Service
         $chatService->getOrCreateSession($this->sessionToken);
         $this->step = 'chat';
+        // Quan trọng: Gửi ID sang JS để thực hiện join room ngay lập tức
+        $this->dispatch('session-joined', id: $session->id);
         $this->dispatch('scroll-bottom');
+
     }
 
     public function send(ChatService $chatService)
diff --git a/Modules/Website/resources/views/livewire/chat/chat-widget.blade.php b/Modules/Website/resources/views/livewire/chat/chat-widget.blade.php
index 6cfafba..a75bb3f 100644
--- a/Modules/Website/resources/views/livewire/chat/chat-widget.blade.php
+++ b/Modules/Website/resources/views/livewire/chat/chat-widget.blade.php
@@ -2,15 +2,16 @@
     <button @click="open = !open"
         class="flex h-14 w-14 items-center justify-center rounded-full bg-blue-600 shadow-lg hover:scale-110 transition-transform text-white">
         <svg x-show="!open" class="h-7 w-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
-            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z"></path>
+            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
+                d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z">
+            </path>
         </svg>
         <svg x-show="open" x-cloak class="h-7 w-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
             <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
         </svg>
     </button>
 
-    <div x-show="open" x-cloak
-        x-transition:enter="transition ease-out duration-200"
+    <div x-show="open" x-cloak x-transition:enter="transition ease-out duration-200"
         x-transition:enter-start="opacity-0 translate-y-4"
         class="absolute bottom-20 right-0 w-80 sm:w-96 h-[500px] bg-white rounded-2xl shadow-2xl border border-gray-100 flex flex-col overflow-hidden">
 
@@ -30,8 +31,10 @@ class="bg-blue-600 text-white px-8 py-3 rounded-xl font-bold text-sm shadow-md h
         @else
             <div id="chat-content" class="flex-1 overflow-y-auto p-4 space-y-4 bg-gray-50 custom-scrollbar">
                 @foreach ($messages as $msg)
-                    <div wire:key="msg-{{ $msg->id }}" class="flex {{ $msg->sender_type != 'admin' ? 'justify-end' : 'justify-start' }}">
-                        <div class="max-w-[85%] p-3 rounded-2xl text-sm shadow-sm
+                    <div wire:key="msg-{{ $msg->id }}"
+                        class="flex {{ $msg->sender_type != 'admin' ? 'justify-end' : 'justify-start' }}">
+                        <div
+                            class="max-w-[85%] p-3 rounded-2xl text-sm shadow-sm
                             {{ $msg->sender_type != 'admin' ? 'bg-blue-600 text-white rounded-br-none' : 'bg-white text-gray-700 border border-gray-100 rounded-bl-none' }}">
                             {{ $msg->message }}
                         </div>
@@ -40,38 +43,140 @@ class="bg-blue-600 text-white px-8 py-3 rounded-xl font-bold text-sm shadow-md h
             </div>
 
             <div class="p-3 bg-white border-t border-gray-100">
-                <form wire:submit.prevent="send" class="flex gap-2">
-                    <input wire:model="message" type="text" placeholder="Nhập tin nhắn..."
-                        class="flex-1 bg-gray-100 border-none rounded-xl px-4 py-2 text-sm focus:ring-2 focus:ring-blue-500">
-                    <button type="submit" class="text-blue-600 hover:scale-110 transition-transform">
-                        <svg class="w-6 h-6 rotate-90" fill="currentColor" viewBox="0 0 20 20"><path d="M10.894 2.553a1 1 0 00-1.788 0l-7 14a1 1 0 001.169 1.409l5-1.429A1 1 0 009 15.571V11a1 1 0 112 0v4.571a1 1 0 00.725.962l5 1.428a1 1 0 001.17-1.408l-7-14z"></path></svg>
-                    </button>
-                </form>
+                <div x-data="{ adminIsTyping: false }"
+                     @display-typing.window="
+                        if($event.detail.session_id == @js($session->id ?? 0) && $event.detail.sender_type == 'admin') {
+                            adminIsTyping = $event.detail.is_typing;
+                        }
+                     "
+                     x-show="adminIsTyping"
+                     x-transition:enter="transition ease-out duration-200"
+                     class="px-5 py-2 text-[11px] text-blue-600 font-medium flex items-center gap-2 bg-gray-50/50 rounded-t-xl">
+                    <div class="flex gap-1">
+                        <span class="w-1 h-1 bg-blue-600 rounded-full animate-bounce"></span>
+                        <span class="w-1 h-1 bg-blue-600 rounded-full animate-bounce [animation-delay:0.2s]"></span>
+                        <span class="w-1 h-1 bg-blue-600 rounded-full animate-bounce [animation-delay:0.4s]"></span>
+                    </div>
+                    Admin đang trả lời...
+                </div>
+
+                <div x-data="{
+                    isTyping: false,
+                    timeout: null,
+                    sendTyping(status) {
+                        if (!window.socket || !window.socket.connected) return;
+                        window.socket.emit('typing', {
+                            session_id: @js($session->id ?? 0),
+                            is_typing: status,
+                            sender_type: 'guest'
+                        });
+                    }
+                }">
+                    <form wire:submit.prevent="send" @submit="sendTyping(false)"
+                          class="relative flex items-center group mt-2">
+
+                        <input type="text" wire:model="message"
+                            @keydown="
+                                if(!isTyping) { isTyping = true; sendTyping(true); }
+                                clearTimeout(timeout);
+                                timeout = setTimeout(() => { isTyping = false; sendTyping(false); }, 3000);
+                            "
+                            class="w-full rounded-2xl border-none bg-gray-100 py-4 pl-5 pr-14 text-sm focus:bg-white focus:ring-2 focus:ring-blue-500 transition-all outline-none"
+                            placeholder="Viết tin nhắn..."
+                            autocomplete="off">
+
+                        <button type="submit" wire:loading.attr="disabled"
+                            class="absolute right-2 h-10 w-10 flex items-center justify-center bg-blue-600 text-white rounded-xl shadow-lg hover:bg-blue-700 transition-all active:scale-95 disabled:opacity-50">
+                            <span wire:loading.remove>
+                                <svg class="h-5 w-5 rotate-45" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path>
+                                </svg>
+                            </span>
+                            <span wire:loading>
+                                <svg class="animate-spin h-4 w-4 text-white" viewBox="0 0 24 24">
+                                    <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
+                                    <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
+                                </svg>
+                            </span>
+                        </button>
+                    </form>
+                </div>
             </div>
         @endif
     </div>
 </div>
-
 @push('scripts')
-<script>
-    document.addEventListener('livewire:initialized', () => {
-        const chatContent = document.getElementById('chat-content');
-        const scroll = () => { if(chatContent) chatContent.scrollTop = chatContent.scrollHeight; };
-        window.addEventListener('scroll-bottom', () => setTimeout(scroll, 50));
-        scroll();
-
-        window.Echo.connector.socket.onAny((eventName, data) => {
-        if (eventName === 'MessageSent') {
-            console.log("📡 Widget nhận tín hiệu, đang làm mới UI...");
-
-            // 'refresh-widget' là listener đã khai báo trong ChatWidget.php
-            Livewire.dispatch('refresh-widget');
-
-            setTimeout(() => {
-                if(chatContent) chatContent.scrollTop = chatContent.scrollHeight;
-            }, 300);
-        }
-    });
-    });
-</script>
+    <script>
+        document.addEventListener('livewire:initialized', () => {
+            const chatContent = document.getElementById('chat-content');
+
+            /**
+             * 1. LOGIC TỰ ĐỘNG CUỘN (SCROLL MANAGEMENT)
+             */
+            const scrollToBottom = () => {
+                if (chatContent) {
+                    chatContent.scrollTop = chatContent.scrollHeight;
+                }
+            };
+
+            // Lắng nghe sự kiện cuộn từ Livewire Widget
+            window.addEventListener('scroll-bottom', () => {
+                setTimeout(scrollToBottom, 100);
+            });
+
+            // Cuộn khi mở widget chat
+            window.addEventListener('chat-opened', () => {
+                setTimeout(scrollToBottom, 300);
+            });
+
+            /**
+             * 2. LẮNG NGHE SỰ KIỆN SOCKET REALTIME (ON ANY)
+             */
+            if (window.Echo && window.Echo.connector.socket) {
+                window.Echo.connector.socket.onAny((eventName, data) => {
+                    // Kiểm tra xem sự kiện có thuộc về phiên chat hiện tại không
+                    const currentSessionId = @js($session->id ?? null);
+                    if (!currentSessionId || data.session_id != currentSessionId) return;
+
+                    // Trường hợp: Có tin nhắn mới từ Admin
+                    if (eventName === 'MessageSent') {
+                        Livewire.dispatch('refresh-widget');
+                    }
+
+                    // Trường hợp: Admin thực hiện xóa tin nhắn
+                    if (eventName === 'MessageDeleted') {
+                        console.log("🗑️ Tin nhắn đã bị Admin thu hồi.");
+                        Livewire.dispatch('refresh-widget');
+                    }
+                });
+            }
+
+            /**
+             * 3. QUẢN LÝ PHÒNG VÀ ĐỊNH DANH (ROOM JOINING)
+             */
+            const joinChatRoom = (id) => {
+                if (window.socket && id) {
+                    window.socket.emit('join-session', id);
+                    console.log("🚪 Widget đã vào phòng chat: session-" + id);
+                }
+            };
+
+            // Lắng nghe lệnh join phòng
+            window.addEventListener('session-joined', (e) => {
+                joinChatRoom(e.detail.id);
+            });
+
+            // Tự động gia nhập phòng khi trang web tải xong
+            @if(isset($session->id))
+                setTimeout(() => {
+                    window.dispatchEvent(new CustomEvent('session-joined', {
+                        detail: { id: @js($session->id) }
+                    }));
+                }, 800); // Trễ một chút để đảm bảo Echo khởi tạo xong
+            @endif
+
+            // Khởi tạo vị trí cuộn ban đầu
+            scrollToBottom();
+        });
+    </script>
 @endpush
diff --git a/resources/js/echo.js b/resources/js/echo.js
index 814d21f..ae56d6c 100644
--- a/resources/js/echo.js
+++ b/resources/js/echo.js
@@ -1,13 +1,14 @@
 import Echo from "laravel-echo";
 import { io } from "socket.io-client";
 
-// Lấy cấu hình từ Window Object (được set trong master layout)
+/**
+ * 1. CẤU HÌNH HOST & SOCKET.IO
+ */
 const SOCKET_HOST = window.CHAT_CONFIG_HOST || window.location.hostname + ":6002";
-
 window.io = io;
 
 /**
- * Khởi tạo Laravel Echo với Driver Socket.io
+ * 2. KHỞI TẠO LARAVEL ECHO
  */
 window.Echo = new Echo({
     broadcaster: "socket.io",
@@ -17,16 +18,42 @@ window.Echo = new Echo({
     withCredentials: false
 });
 
-// Log trạng thái phục vụ debug
+/**
+ * 3. HÀM KHỞI TẠO SỰ KIỆN (AN TOÀN)
+ * Đảm bảo connector.socket đã tồn tại trước khi đăng ký listener
+ */
+// resources/js/echo.js
+const initSocketEvents = () => {
+    const socket = window.Echo.connector.socket;
+    if (!socket) {
+        setTimeout(initSocketEvents, 100);
+        return;
+    }
+    window.socket = socket;
+
+    // QUAN TRỌNG: Đây là cầu nối giúp AlpineJS bắt được tín hiệu
+    socket.on("display-typing", (payload) => {
+        console.log("⌨️ Socket nhận display-typing từ server:", payload);
+        window.dispatchEvent(new CustomEvent("display-typing", {
+            detail: payload
+        }));
+    });
+};
+initSocketEvents();
+
+// resources/js/echo.js
+
 window.Echo.connector.socket.on("connect", () => {
-    console.log("✅ SOCKET_HOST: ", SOCKET_HOST);
-    console.log("✅ Chat Realtime Connected. ID:", window.Echo.connector.socket.id);
-});
+    console.log("✅ Socket đã kết nối. ID:", window.Echo.connector.socket.id);
 
-window.Echo.connector.socket.on("connect_error", (error) => {
-    console.error("❌ Chat Connection Error:", error);
+    // Đăng ký lại Listener ngay khi vừa kết nối
+    window.Echo.connector.socket.off("display-typing"); // Xóa bản cũ nếu có
+    window.Echo.connector.socket.on("display-typing", (payload) => {
+        console.log("⌨️ NHẬN ĐƯỢC TÍN HIỆU TYPING:", payload);
+        window.dispatchEvent(new CustomEvent("display-typing", { detail: payload }));
+    });
 });
 
-window.Echo.connector.socket.onAny((eventName, data) => {
-    console.log("🔔 Tín hiệu thô nhận được từ Socket:", eventName, data);
-});
+window.socket = window.Echo.connector.socket;
+
+export default window.Echo;
diff --git a/socket/server.js b/socket/server.js
index 5852fd7..0292483 100644
--- a/socket/server.js
+++ b/socket/server.js
@@ -3,10 +3,14 @@ const cors = require('cors');
 const express = require("express");
 const { createServer } = require("http");
 const { Server } = require("socket.io");
+const fs = require("fs");
+const path = require("path");
 
 const app = express();
-app.use(cors());
-app.use(express.json());
+
+// --- 1. MIDDLEWARE HỆ THỐNG ---
+app.use(cors()); // Cho phép các domain truy cập (Cấu hình cụ thể hơn ở production)
+app.use(express.json()); // Để đọc dữ liệu JSON từ Laravel Bridge
 
 const httpServer = createServer(app);
 const io = new Server(httpServer, {
@@ -16,30 +20,44 @@ const io = new Server(httpServer, {
     }
 });
 
-/**
- * Middleware: Bảo mật kênh truyền từ Laravel
- */
+// --- 2. MIDDLEWARE BẢO MẬT (BRIDGE AUTH) ---
+// Kiểm tra mã bí mật từ Laravel gửi sang để tránh giả mạo tin nhắn
 const bridgeAuth = (req, res, next) => {
     const secret = req.headers['x-bridge-secret'];
-    if (secret !== process.env.BRIDGE_SECRET_KEY) {
+    const validSecret = process.env.BRIDGE_SECRET_KEY;
+
+    if (!secret || secret !== validSecret) {
+        console.error(`⚠️ Unauthorized access attempt from IP: ${req.ip}`);
         return res.status(401).json({ error: 'Unauthorized bridge request' });
     }
     next();
 };
 
-// Route nhận tin nhắn từ Laravel Bridge (Đã thêm Auth)
-app.post("/broadcast", bridgeAuth, (req, res) => {
-    const { channel, event, data } = req.body;
-
-    // Phát tín hiệu tới đúng channel hoặc broadcast toàn cục
-    // Ở bản này, chúng ta dùng emit đơn giản, có thể mở rộng sang rooms
-    io.emit(event, data);
+// --- 3. CƠ CHẾ AUTO-LOAD MODULES (CORE) ---
+// Quét toàn bộ thư mục 'events/' và nạp các logic nghiệp vụ
+const eventsPath = path.join(__dirname, "events");
 
-    res.json({ ok: true });
-});
+if (fs.existsSync(eventsPath)) {
+    console.log("📂 Scanning for event modules...");
+    fs.readdirSync(eventsPath).forEach(file => {
+        if (file.endsWith(".js")) {
+            try {
+                // Nạp file và truyền vào các tham số cần thiết
+                const eventModule = require(`./events/${file}`);
+                eventModule(io, bridgeAuth, app);
+                console.log(`✅ Module loaded: ${file}`);
+            } catch (error) {
+                console.error(`❌ Failed to load module ${file}:`, error.message);
+            }
+        }
+    });
+} else {
+    console.warn("⚠️ Directory 'events/' not found. Creating one is recommended.");
+}
 
+// --- 4. SOCKET CONNECTION LOGIC ---
 io.on("connection", (socket) => {
-    console.log("🔌 Client connected:", socket.id);
+    console.log(`🔌 Client connected: ${socket.id}`);
 
     socket.on("join", (room) => {
         socket.join(room);
@@ -47,11 +65,17 @@ io.on("connection", (socket) => {
     });
 
     socket.on("disconnect", () => {
-        console.log("❌ Client disconnected:", socket.id);
+        console.log(`❌ Client disconnected: ${socket.id}`);
     });
 });
 
+// --- 5. START SERVER ---
+app.get("/", (req, res) => res.send("🚀 Socket server is running..."));
+
 const PORT = process.env.PORT || 6002;
 httpServer.listen(PORT, "0.0.0.0", () => {
-    console.log(`🚀 Realtime Server running at port ${PORT}`);
+    console.log(`-------------------------------------------`);
+    console.log(`🚀 REALTIME SERVER RUNNING AT PORT: ${PORT}`);
+    console.log(`🛡️  Bridge Auth: ENABLED`);
+    console.log(`-------------------------------------------`);
 });
